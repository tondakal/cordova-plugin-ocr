repositories{    
  jcenter()
  flatDir{
      dirs 'src/android/'
   }
}

dependencies {
   compile(name:'ocr', ext:'aar')
}

android {
  packagingOptions {
    exclude 'META-INF/NOTICE'
    exclude 'META-INF/LICENSE'
  }
}